# ATDD 文档模板

## 用途
本模板定义了ATDD（验收测试驱动开发）文档的结构。ATDD文档分为两个阶段：
- **初稿阶段**（阶段二）：在编码之前，根据BDD场景规划验收测试用例，供用户审阅确认。
- **定稿阶段**（阶段五）：在自验证完成后，将实际验证结果和证据填入文档。

ATDD文档与BDD场景直接对应和追溯。

## 文档结构

每个ATDD文档**必须**遵循以下结构：
```markdown
# [功能名称]

## BDD参考
[BDD文档路径，如 PROJECT_STATE/BDD/user-email-login.md]

## 验证策略
- 日期：[验证日期]
- 策略：[一种或多种：运行测试框架、调用工具(MCP/skill)、执行校验命令、编写测试脚本、构建/运行项目...]
- 方式：[一种或多种：检查测试报告、工具调用结果、命令/脚本运行结果、项目构建产物、项目运行状态...]

## 测试用例

### 测试用例 1：[测试名称]
**对应BDD场景**：[场景名称，如"使用有效凭据成功登录"]
**验证者**：Agent自动验证 / 人工验证（仅人工可验证的场景，如视觉动效、跨系统验证、比对设计图稿等）

**验证思路**：[获取证据的思路和过程——如何验证该场景的行为，包含具体的操作路径、验证方法、观测点。如为人工验证，说明需要人关注什么]
**测试所用命令**（如果有的话）：[具体命令]
**测试所用的工具**（如果有的话）：[工具名称]

**结果**：通过 / 失败（初稿阶段留空）
**证据**：[确认结果的内容。当变更对人可见时（UI/API/数据输出等），**必须**包含人类可直观检验的证据（如改动后关键截图、实际请求/响应示例、输入/输出对照），而非仅提供测试通过信息]（初稿阶段留空）

---

### 测试用例 2：[测试名称]
...

## 汇总

| BDD场景 | 测试用例 | 验证者 | 结果 |
|---|---|---|---|
| [场景名称] | TC-1 | Agent / 人工 | （初稿阶段留空） |
| ... | ... | ... | ... |

**场景总数**：[N]
**通过数**：[N]（初稿阶段留空）
**失败数**：[N]（初稿阶段留空）
**备注**：[任何限制条件、跳过的场景或无法完全验证的条件]
```

## 非文字证据

自验证过程中产生的非文字证据（截图、数据文件等）应保存到
`PROJECT_STATE/ATDD/<特性名称>/`目录下，使用有意义的文件名。
当变更对人可见时，应主动采集变更后的关键截图作为证据，命名建议：
- 截图：`tc1-homepage.jpeg`、`tc2-result.jpeg`（用例编号+描述性名称）
- 通用证据：`evidence-tc3.log`、`evidence-tc3.json`

在acceptance.md的证据字段中使用相对路径引用：
- **图片类证据**（`.jpeg`、`.png`、`.gif`）使用Markdown图片语法，便于直接预览：
  ```
  ![首页截图](./tc1-homepage.jpeg)
  ```
- **非图片类证据**（`.log`、`.json`、`.csv`等）使用普通链接语法：
  ```
  详见 [响应数据](./evidence-tc3.json)
  ```

支持的证据文件类型：
- 截图：`.jpeg`、`.png`、`.gif`
- 数据文件：`.json`、`.csv`、`.xml`
- 其他：任何能说明验证结果的文件

## 编写指南

### 初稿阶段（阶段二）
1. 每个测试用例通过"**对应BDD场景**"字段追溯其来源场景，不重复 Given/When/Then。
2. **验证思路**描述获取证据的思路和过程——具体的操作路径、验证方法、观测点。
3. 包含**计划使用**的测试命令或工具（即使尚未编写测试代码，也应描述预期的命令格式）。
4. 结果和证据字段留空，标注"（待验证）"。
5. 汇总表中的结果列留空。

### BDD 与 ATDD 的职责分工

BDD 和 ATDD 回答的是不同的问题，不应重复相同内容：

- **BDD 回答"期望什么行为"**——用 Given/When/Then 以业务语言描述系统的前置条件、操作和预期结果
- **ATDD 回答"如何验证该行为"**——通过验证思路、测试命令/工具、结果和证据，描述具体的验证方法

ATDD 通过"对应BDD场景"字段追溯 BDD 场景，不再重复描述功能行为，而是专注于验证层面。

**示例对比**——同一个场景在两份文档中的呈现：
- BDD 中：`Given 系统中已注册用户 alice@example.com` / `When 用户提交登录请求` / `Then 登录成功，系统返回认证令牌`
- ATDD 中：`对应BDD场景：使用有效凭据成功登录` / `验证思路：使用pytest发送POST请求，断言状态码200且含token字段`

### 定稿阶段（阶段五）
1. 根据实际自验证结果填写每个测试用例的**结果**（通过/失败）。
2. 填写每个测试用例的**证据**（测试输出、日志片段/数据、截图等）。
3. **检查人类可验证性**：对每个测试用例，审视其证据是否允许人不读代码就判断结果是否正确。当变更对人可见时（UI/API/数据输出等），**必须**包含关键截图（改动后截图、实际请求/响应示例、输入/输出对照等），达到证据等级S。
4. 如有非文字证据文件，在证据字段中使用相对路径引用。**图片类证据使用Markdown图片语法**（如 `![首页截图](./evidence-tc1.jpeg)`），非图片类证据使用链接语法（如 `详见 [日志](./evidence-tc2.log)`）。截图建议使用 `tc<编号>-<描述>.jpeg` 格式命名。
5. 填写汇总表。
6. 如实记录结果。如果某个场景无法通过自动化测试验证的，如实说明并描述检查了哪些内容。
7. 在备注中说明任何限制条件。

## 完整示例（定稿后）

```markdown
# 用户邮箱登录

## BDD参考
PROJECT_STATE/BDD/user-email-login.md

## 验证策略
- 日期：2026-04-09
- 策略：测试框架-pytest (v8.1)
- 方式：自动化单元测试 + API调用结果检查

## 测试用例

### 测试用例 1：有效凭据登录成功
**对应BDD场景**：使用有效凭据成功登录
**验证者**：Agent自动验证

**验证思路**：使用pytest发送POST /api/login请求（请求体包含邮箱和密码），断言响应状态码200且响应体包含token字段，解码JWT验证exp声明与24小时有效期一致
**测试所用命令**：`pytest tests/test_login.py::test_valid_login -v`

**结果**：通过
**证据**：测试输出显示 "assert response.status_code == 200" 断言通过。JWT解码后exp声明正确。
![测试输出截图](./evidence-tc1.jpeg)

---

### 测试用例 2：格式错误的邮箱被拒绝
**对应BDD场景**：使用格式错误的邮箱登录
**验证者**：Agent自动验证

**验证思路**：使用pytest发送POST /api/login请求（邮箱为非法格式），断言响应状态码400且响应体包含"Invalid email format"错误信息，确认验证层在数据库查询前即拒绝
**测试所用命令**：`pytest tests/test_login.py::test_malformed_email -v`

**结果**：通过
**证据**：验证层在数据库查询之前即拒绝了请求。
![测试输出截图](./evidence-tc2.jpeg)

---

### 测试用例 3：数据库不可达时返回服务不可用错误
**对应BDD场景**：系统服务不可用时登录
**验证者**：Agent自动验证

**验证思路**：通过docker stop停止数据库容器模拟不可用状态，使用curl发送有效凭据的登录请求，检查响应状态码503和错误信息是否为"Service temporarily unavailable"，验证后重启数据库
**测试所用的工具**：curl, docker

**结果**：通过
**证据**：停止数据库后，通过curl发送请求。收到503响应及正确的错误信息体。测试后重启数据库。
![响应截图](./evidence-tc3.jpeg)

---

### 测试用例 4：登录页面视觉呈现正确
**对应BDD场景**：使用有效凭据成功登录（视觉验证部分）
**验证者**：人工验证

**验证思路**：检查登录页面的视觉呈现——输入框对齐、按钮样式、错误提示的颜色和位置是否与设计稿一致。Agent提供页面截图供人确认。
**需要人关注的要点**：输入框间距、错误提示文字颜色是否为红色、整体布局是否与设计稿一致

**结果**：待人工确认
**证据**：
![登录页面截图](./evidence-tc4-login-page.jpeg)

---

## 汇总

| BDD场景 | 测试用例 | 验证者 | 结果 |
|---|---|---|---|
| 使用有效凭据成功登录 | TC-1 | Agent | 通过 |
| 使用格式错误的邮箱登录 | TC-2 | Agent | 通过 |
| 系统服务不可用时登录 | TC-3 | Agent | 通过 |
| 登录页面视觉呈现正确 | TC-4 | 人工 | 待人工确认 |

**场景总数**：4
**通过数**：3
**失败数**：0
**待人工确认**：1
```

## 完整示例（初稿状态）

初稿阶段，结果和证据字段留空：

```markdown
# 用户邮箱登录

## BDD参考
PROJECT_STATE/BDD/user-email-login.md

## 验证策略
- 日期：2026-04-09
- 策略：测试框架-pytest (v8.1)
- 方式：自动化单元测试 + API调用结果检查

## 测试用例

### 测试用例 1：有效凭据登录成功
**对应BDD场景**：使用有效凭据成功登录
**验证者**：Agent自动验证

**验证思路**：使用pytest发送POST /api/login请求（请求体包含邮箱和密码），断言响应状态码200且响应体包含token字段，解码JWT验证exp声明与24小时有效期一致
**测试所用命令**（计划）：`pytest tests/test_login.py::test_valid_login -v`

**结果**：（待验证）
**证据**：（待验证）

---

### 测试用例 2：格式错误的邮箱被拒绝
**对应BDD场景**：使用格式错误的邮箱登录
**验证者**：Agent自动验证

**验证思路**：使用pytest发送POST /api/login请求（邮箱为非法格式），断言响应状态码400且响应体包含"Invalid email format"错误信息
**测试所用命令**（计划）：`pytest tests/test_login.py::test_malformed_email -v`

**结果**：（待验证）
**证据**：（待验证）

---

### 测试用例 3：数据库不可达时返回服务不可用错误
**对应BDD场景**：系统服务不可用时登录
**验证者**：Agent自动验证

**验证思路**：通过docker stop停止数据库容器模拟不可用状态，使用curl发送有效凭据的登录请求，检查响应状态码503和错误信息
**测试所用的工具**（计划）：curl, docker

**结果**：（待验证）
**证据**：（待验证）

---

### 测试用例 4：登录页面视觉呈现正确
**对应BDD场景**：使用有效凭据成功登录（视觉验证部分）
**验证者**：人工验证

**验证思路**：检查登录页面的视觉呈现——输入框对齐、按钮样式、错误提示的颜色和位置是否与设计稿一致。Agent提供页面截图供人确认
**需要人关注的要点**：输入框间距、错误提示文字颜色是否为红色、整体布局是否与设计稿一致

**结果**：（待人工确认）
**证据**：（待采集）

---

## 汇总

| BDD场景 | 测试用例 | 验证者 | 结果 |
|---|---|---|---|
| 使用有效凭据成功登录 | TC-1 | Agent | （待验证） |
| 使用格式错误的邮箱登录 | TC-2 | Agent | （待验证） |
| 系统服务不可用时登录 | TC-3 | Agent | （待验证） |
| 登录页面视觉呈现正确 | TC-4 | 人工 | （待人工确认） |

**场景总数**：4
**通过数**：（待验证）
**失败数**：（待验证）
**待人工确认**：1
```
